static
   address 192.168.0.50
   netmask 255.255.255.0
   network 192.168.0.0
   gateway 192.168.0.1

sudo if-down eth0 && sudo if-up eth0   dns-nameservers 8.8.8.8

Commands for bp-test

The vagrant application will be run with Virtuabox from Oracle
It is assumed that you are running on a host machine with native O/S connected 
to the internet and that your vagrant guest machines will be operational on that host


